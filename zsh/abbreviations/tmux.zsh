# ==============================================================================
# Tmux Abbreviations
# ==============================================================================

abbr -S tls='tmux ls'
abbr -S ta='tmux attach -t'
abbr -S tn='tmux new -s'
abbr -S tk='tmux kill-session -t'
