# ==============================================================================
# Safety Aliases
# ==============================================================================
# These aliases add confirmation prompts to destructive commands
# They should NOT be converted to abbreviations - we want 'rm' to stay as 'rm'
# in command history, not expand to 'rm -i'

alias cp='cp -i'
